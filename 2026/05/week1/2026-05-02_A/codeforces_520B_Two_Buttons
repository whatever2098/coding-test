#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	int n, m;
	cin >> n >> m;

	int cnt{ 0 };

	while (n < m) {
		if (m % 2 == 0) {
			m /= 2;
		}
		else {
			m++;
		}
		cnt++;
	}
	cnt += (n - m);

	cout << cnt << "\n";
}